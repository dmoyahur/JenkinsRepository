pipeline
{
  agent any
  stages
  {
    stage("Conexión con Jenkins")
    {
      steps 
      {
        echo "Java version:"
        sh "java --version"
        echo "Git version:"
        sh "git --version"
      }
    }  
  }
}
